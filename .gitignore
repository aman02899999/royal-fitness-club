# Firebase CLI cache and logs
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Service account keys — never commit credentials
*serviceaccount*.json
*service-account*.json
*firebase-adminsdk*.json
sa-key.json

# Node
node_modules/
npm-debug.log

# OS
.DS_Store

# Android build outputs (not needed in git — CI builds these)
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/app/rfc-release-key.jks
*.jks
*.keystore

# Capacitor www dist (auto-regenerated by npm run update:app)
www/

# Reel frame captures (raw PNGs — large binary)
reel-frames/

# Reel frame build artifacts
reel-frames/
reel60-frames/

# Build/capture scripts (one-off tooling)
capture-screens.js
make-poster.js
record-reel.js
record-reel60.js
reel60.html
